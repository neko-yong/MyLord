# P0 最小现场取证方案

状态：本地候选诊断已实现但未部署；本地候选门禁 PASS，生产关闭仍为 CONDITIONAL FAIL。

代码检查点：`a1cc946584d337b7604aa7eee887a314347120a6`。
本轮在隔离分支加入默认关闭的安全阶段记录、失败优先回归与一个最小渲染顺序修复；不修改认证、数据库结构、部署或真实配置，不 amend 原检查点。

## 本轮边界与结果

| 场景 | 本轮状态 | 原因 |
| --- | --- | --- |
| 真实确认弹窗执行期间，仅断开浏览器连接、保留后端运行，再恢复 | 未测 | 当前已暴露、已使用的浏览器控制接口没有受支持的逐连接离线/恢复控制；无法核验 WebSocket 确实断开。 |
| A/B 分别切到系统后台、返回前台后的同步 | 未测 | 当前接口没有受支持的系统窗口前后台/睡眠控制与相应验证。双标签会话不等于这一场景。 |
| 普通双客户端、慢模型、依赖失败、重复确认、生成所有者切换 | 不重复 | 已有检查点证据；再次执行相同流程不会补足上述缺口。 |

本轮没有启动测试服务。浏览器标签列表为空只是之前测试清理后的正常状态，不能视为连接故障或空白页面证据。没有使用私有 CDP、浏览器内部状态、系统网络修改或新建代理来绕过工具限制。

禁止用以下替代操作宣称完成断连测试：硬刷新、关闭/新开标签、停启内存夹具服务、仅观察离线图标。这些操作可能创建新会话、清空虚构数据库，或者并未真正关闭目标 WebSocket。目标断连必须有独立连接关闭/重连证据，且后端同一进程仍存活。

## 只读框架审计：线索，不是根因结论

本地已安装 Streamlit 1.62.0 的 `runtime/websocket_session_manager.py`：

- `disconnect_session` 请求停止脚本，并把断开的会话移入存储。
- `connect_session` 根据旧会话是否仍可用、框架身份匹配等条件恢复旧会话，否则建立新会话。
- 本地框架 `server.disconnectedSessionTTL` 默认是 120 秒；配置说明指出复制/负载均衡部署还涉及会话黏性。未读取或确认实际部署配置，不能把本地默认值当成现场值，更不能据此直接调整 TTL。

这解释了为何取证要区分“同一进程 / 同一诊断会话 / 新脚本执行 / 新客户端连接”。它不证明生产发生过断连，也不证明已有 P0 就由这一分支导致。框架身份检查也不等于本项目 A/B 参与者认证；不能替换或绕过现有认证规则。

已知地图修补解决的是 spinner 清理前未落结果/失败标记的问题。最终仲裁已在 spinner 内保存，不能把地图根因直接套用到最终仲裁。

## 还缺的非敏感信息

用户已经提供以下信息，不再重复索要：

- 公开主页：[应用主页](https://mylord-wrj9cnafjmvupnbsulyvvg.streamlit.app/)（无参数、无片段）。
- 设备：联想拯救者 Y9000P。
- 浏览器：Chrome；具体版本未提供。电脑型号不能用来推断操作系统、浏览器版本或故障归因。

2026-08-31 的公开页只读检查未取得页面内容：浏览器导航先返回 `net::ERR_CONNECTION_CLOSED`；辅助网页读取通道随后连接失败；对同一浏览器页的后续只读观察明确被 Browser URL 安全策略拦截。策略拦截后已停止所有针对该网址的访问，没有换浏览器、代理、脚本或探测其他路径。此前的辅助请求发生在明确策略拦截之前，不是取得页面的替代成功证据。

因此：当前通道下页面可达性未确认；登录前页面内容未读取到；线上 Streamlit 版本、线上部署提交、Chrome 具体版本仍未确认。不能把工具访问失败当成站点宕机、案件内空白或已确认的生产网络故障，也没有去登录、创建或操作线上案件。

本地与现场目前可区分的条件：

| 条件 | 本地已验证环境 | 实际故障环境 |
| --- | --- | --- |
| 浏览器 | 内嵌 Chromium 测试通道 | 用户称 Chrome，版本未知；不能视为完全相同构建。 |
| 页面链路 | 127.0.0.1 本地服务 | 用户提供的远端 HTTPS 主页；本轮未取得页面，具体连接/代理行为未知。 |
| 服务端版本 | Streamlit 1.62.0 | 未确认。当前本地及基线 requirements 都是 `streamlit>=1.62,<2`，没有精确锁定到 1.62.0；不证明实际部署用了这份依赖或此版本。 |
| 代码版本 | 基线 bef2e47d；有限修补 a1cc946 | 部署提交未确认，不能因为本地 main 已知就假定线上相同。 |
| 数据/模型 | 虚构内存数据、模拟模型、受控延迟 | 未读取实际配置或数据；不得用真实 API/案件补测。 |

优先级因此是“确认实际版本与部署代码对应关系，再取得同轮连接—渲染证据”，而不是修改 Y9000P、浏览器设置或认证架构。远端链路与不同浏览器构建是待排查差异，不是已证明根因。用户原有的两条确认后故障路径仍是主要验收路径。

维护者侧还需确认：

1. 受影响部署的代码版本、Streamlit 版本、Chrome 具体版本；只需版本/提交信息，不要配置文件或凭据。公开页没有给出可用版本证据，保持未确认。
2. 是否多进程/多实例、是否存在代理、是否发生进程重启或实例切换；只需类型/布尔值及诊断用匿名启动编号，不要内部地址、主机名、账号或连接配置。
3. 故障时间与时区；是第一人还是最后一人提交，按钮显示名称，确认到切板块的大致间隔；是网页内部切板块、浏览器换标签，还是切到其他软件/锁屏。
4. 故障时标签栏是否存在、是否仍有 spinner/错误/重连提示、另一端是否正常、何时恢复。记录观察事实，不依靠“卡死”这一概括词分类。

不要收集 KID/Case ID、A/B 密钥、API、DSN、真实案情、陈述、聊天、证据、提示词、完整 DOM、完整日志包、完整 HAR 或 WebSocket 消息正文。

## 最小操作：先有一轮同源证据

任何线上启用诊断、增加事件或变更部署都须另行确认。优先在获授权的隔离环境，用虚构案件与模拟模型复现；本方案本身不构成部署授权。

两条独立流程，每条先取一轮，不预先做大量重复：

- A 路径：空白案件 → 两端实际填写陈述 → 实际确认冻结 → 生成中切“②争议地图”和“③调解室”。分别记录最后提交者与先提交者；不要预制完成地图。
- B 路径：实际调解 → 一方申请 → 另一方在真实弹窗确认 → 立即同时观察两端，直到模型/保存结束。

若获授权的测试端能真实制造断连，再分别增加“模型仍运行时断连后恢复”和“模型完成后才恢复”各一次；记录断开时长与服务端进程是否不变。若连接关闭无法证实，则标未执行，不拿离线开关状态替代。

若能真实控制前后台，再分别增加确认端离开前台/返回、另一端在其后台期间发生修订；记录可见性转换时间。不要在同一首轮同时叠加断连、后台、重启，避免无法区分触发条件。

统一观察点：确认点击前、点击后约 0.5 秒、2 秒、模型结束时、结束后约 5 秒。记录以下窄字段即可：

`UTC 时间 / 流程 A或B / 客户端别名甲或乙 / 当前板块枚举 / 标签组存在 / 内容区存在 / spinner存在 / 弹窗存在 / 错误或重连提示存在 / 认证存在`

截图只裁取不含案件标题、案件编号、正文和密钥的导航/状态区域；能记录上述布尔值时不必上传截图。故障后先保留这轮观察，再做一次已约定的刷新恢复对照；刷新成功不算修复。

## 默认关闭的安全事件：本地候选与仍缺项分开

候选 `RERUN_STATE_TRACE` 默认为关闭，并且只有同时处于 `DEV_MODE` 或 `DEVELOPMENT_MODE` 才能启用。它记录脚本/fragment、确认收到、动作落库、pending 清理、整页 rerun、新 run、快照、自动地图、live sync 判定、tabs 注册、选中分支、模型、冻结、保存与 render complete。字段只允许动作/结果枚举、布尔值、递增序号、耗时、四位 tabs flags 和 revision 摘要；不接受 Case ID、token、正文、URL、原始 session/API/DSN。它只说明服务端执行到了哪里，`render_complete` 不是浏览器接收确认。顶层 interrupted 记录仍只在虚构浏览器夹具中；不可描述为已上线能力。

下表中“连接、客户端观察、可见性”仍未实现；服务端确认至 render complete 的阶段已经在本地候选实现但从未上线：

| 边界 | 最小事件/字段 | 用途 |
| --- | --- | --- |
| 连接 | connect / disconnect / reconnect_result；结果枚举 resumed/new/unknown；匿名进程启动编号 | 区分重连旧会话与换进程/新会话，不记录真实 session ID。 |
| 用户动作 | confirmation_click；action 枚举 statement/arbitration_accept；客户端别名与单轮诊断编号 | 证实点击被客户端和服务器哪一侧接收，不记录表单数据。 |
| 脚本退出 | run_finished；outcome 枚举 complete/rerun/stop/error；固定错误类别 | 区分正常结束、重跑、断连停止和异常，不记录异常正文或完整堆栈。 |
| 未完成对话框 | pending_action_kind 枚举或 none；dialog_rendered 布尔值 | 判断状态已提交但旧确认上下文是否仍保留。 |
| 保存 | persist_attempt / persist_finished；success/failure；任务枚举、尝试序号 | 避免把调用开始当成成功；不记录结果正文。 |
| UI 发出 | render_complete + run_seq + rendered_view + 四位 flags | 明确这是服务端输出完成，不承诺发送/接收成功。 |
| UI 观察 | client_dom_observed；对应 run_seq 或诊断标记；可见板块枚举及布尔值 | 与同一轮服务端输出匹配；DOM 标记不等于认证令牌。 |
| 可见性/链路 | client_visibility visible/hidden；连接 opened/closed/reconnecting 的时间 | 判断返回前台后是否继续接收；online/offline 信号只作线索。 |

关联规则：UTC 时间 + 单调耗时、每进程随机匿名启动编号、独立于鉴权的随机诊断会话编号、逐次执行序号。诊断编号只留内存，不使用原始 token、真实 Streamlit session ID、Case ID 或其可逆表示，不写 URL/localStorage/cookie。

事件只接受固定枚举、布尔、序号、时间和既有不含正文的修订摘要。默认关闭，开启时限定一次虚构复现与有限日志窗口；关闭后不增加查询或常驻任务。维护者导出前先做字段白名单筛选，不要求用户上传原始日志。

现有受支持工具不能实现某个边界时，记录 unknown。不得把“没有采到事件”直接解释成“事件没有发生”。本轮不为补事件修改框架私有实现或认证架构。

## 如何区分服务端未渲染与客户端未收到

| 同一轮可核对的证据 | 能得出的结论 | 仍不能声称 |
| --- | --- | --- |
| 模型/DB started 后无完成；有停止/异常或仍在执行证据 | 工作尚未完成或被中断，继续定位该阶段 | 已经是浏览器渲染 bug |
| 保存成功，但没有下一次 snapshot/tabs/render_complete；随后 stop/disconnect | 业务已提交，服务端恢复渲染路径未完成 | 浏览器丢了已发完的页面 |
| render_complete 存在，但缺少对应客户端观察标记 | 问题位于服务端发出之后至客户端观察之间 | 仅凭服务端日志就断言网络丢包或前端渲染失败 |
| 客户端记录了对应渲染标记，标签/内容计数异常，连接保持 | 才可优先检查前端应用 delta/清理/可见性路径 | 与旧轮次标记混用后直接确认根因 |
| 重连结果 new，认证存在标志改变 | 发生会话替换，核对 TTL/路由/重启等证据 | 正常 rerun 主动清除了认证，或应持久化原始密钥 |

如要把“未收到”与“收到但未呈现”进一步分开，还需可信的同轮传输发送/接收元数据，仅计数、序号、时间、关闭码；不导出帧内容。没有这一层时保留为“传输至客户端呈现之间未定位”。禁止通过完整 HAR/WS payload 换取判断依据。

## 停止与移交

本轮新增的是现场记录的脱敏事实、可重复的等价时序证据和 loopback 浏览器证据，不是真实生产 telemetry。三条真实弹窗路径各 10 次、双会话导航与单次副作用均通过；完整离线门禁共 195 项，其中 180 通过、15 项按规则跳过。本地候选可进入集成评审，但不能据此宣告生产已关闭。

最小下一步是补齐版本与部署对应关系，并由维护者在另行授权的 canary/隔离部署完成一次同轮脱敏 phase trace；手机或同等真实设备再执行后台/返回前台人工检查。主页、设备型号及浏览器名称已经记录，不再重复索要。线上诊断启用、部署和身份持久化仍未放行；本地候选和网址补充都不构成生产变更批准。
